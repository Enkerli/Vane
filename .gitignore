build*/
.DS_Store
*.xcuserstate
xcuserdata/
JUCE

# macOS/tool artifact in design folder
Vane Design/.thumbnail
.thumbnail

# WebUI source now lives in the music-suite monorepo (apps/vane); only the C++
# editor (WebVaneEditor.*) remains under Source/WebUI here.
webui.local.cmake
